auxiliary Field FIELD

hostdefine STENCIL_ORDER (0)

enum MY_STEP_NUM
{
	FIRST,
	SECOND
}
Kernel singlepass_solve(MY_STEP_NUM step_num) {
    write(FIELD, 1.0)
}

BoundConds bcs
{
	periodic(BOUNDARY_XYZ)
}
ComputeSteps rhs(bcs)
{
	singlepass_solve((MY_STEP_NUM)0)
}
