[project]
name = "AstarothPythonPipeline"
version = "0.1.0"
description = "Astaroth Python visualization and analysis tools."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "isort>=8.0.1",
    "matplotlib",
    "pandas",
]
