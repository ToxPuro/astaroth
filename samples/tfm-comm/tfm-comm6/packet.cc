#include "packet.h"

#include "print_debug.h"

void
test_packet(void)
{
    // Shape dims{2, 2, 2};
    // Index offset{0, 0, 0};
    // Segment segment(dims, offset);
    // Packet<double> packet(segment, 2);
    // PRINT_DEBUG(packet);
}
