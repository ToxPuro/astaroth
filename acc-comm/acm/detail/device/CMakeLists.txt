if (CUDA_ENABLED)
    set_source_files_properties(pack.cu PROPERTIES LANGUAGE HIP)
    set(CMAKE_HIP_ARCHITECTURES "gfx90a;gfx908")
    set_property(TARGET acm-device-libraries PROPERTY HIP_ARCHITECTURES ${CMAKE_HIP_ARCHITECTURES})
    add_library(acm-device-libraries STATIC pack.cu)
elseif(HIP_ENABLED)
    set_source_files_properties(pack.cu PROPERTIES LANGUAGE CUDA)
    set(CMAKE_CUDA_ARCHITECTURES "60;61;70;80")
    set_property(TARGET acm-device-libraries PROPERTY CUDA_ARCHITECTURES ${CMAKE_CUDA_ARCHITECTURES})
    add_library(acm-device-libraries STATIC pack.cu)
else()
    add_library(acm-device-libraries INTERFACE)
endif()
