#include "prerequisites.h"

Kernel solve() {
  write( UU, rk3(previous(UU), value(UU), dUU_dt()))
  write( RHO, rk3(previous(RHO), value(RHO), dRHO_dt()))
  write( AA, rk3(previous(AA), value(AA), dAA_dt()))
  write( SS, rk3(previous(SS), value(SS), dSS_dt()))
}
